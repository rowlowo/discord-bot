{
  "name": "discbot",
  "version": "1.0.0",
  "description": "Bot that will respond with youtube video based on what the user types in the chat for discord servers",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "rowlowo",
  "license": "MIT",
  "dependencies": {
    "discord.js": "^11.0.0",
    "request": "^2.79.0"
  }
}
